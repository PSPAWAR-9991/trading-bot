services:
  - type: web
    name: trading-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: REQUEST_TOKEN
        sync: false
      - key: ACCESS_TOKEN
        sync: false
